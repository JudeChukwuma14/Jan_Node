<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>NexusPlus - Cart</title>

    <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

    <header id="header-wrap">
        {{> navbars}}
    </header>

    <div class="page-header" style="background: url(assets/img/banner1.jpg);">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="breadcrumb-wrapper">
                        <h2 class="product-title">Cart Page</h2>
                        <ol class="breadcrumb">
                            <li><a href="/">Home /</a></li>
                            <li class="current">Cart</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </div>
<div class="container my-5">
    <h2 class="mb-4">Shopping Cart</h2>

    <div class="cart">
        {{#each cart}}
        <div class="cart-item d-flex justify-content-between align-items-center border-bottom py-3" data-cart-id="{{_id}}">
            <div class="d-flex align-items-center">
                <img src="{{newImage}}" alt="Product" class="cart-img">
                <span class="ms-3">{{title}}</span>
            </div>
            <span>${{price}}</span>
            <input type="number" class="form-control qty-input" value="{{quantity}}" min="1" data-price="{{price}}">
            <span class="item-total">${{total}}</span>
            <button class="btn btn-sm btn-outline-danger remove-item">Remove</button>
        </div>
        {{/each}}
    </div>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <h4>Total: $<span id="totalAmount">{{totalAmount}}</span></h4>
        <button class="btn btn-primary"><a href="/checkout" style="text-decoration: none;">Checkout</a></button>
    </div>
</div>

    <style>
        .cart-img {
            width: 50px;
            height: 50px;
            object-fit: cover;
            border-radius: 5px;
        }

        .qty-input {
            width: 60px;
            text-align: center;
        }
    </style>
    







</body>

</html>